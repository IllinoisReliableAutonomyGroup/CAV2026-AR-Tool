\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    node distance=1.5cm and 2.5cm,
    every node/.style={draw, align=center, rounded corners, minimum height=1cm},
    input/.style={fill=blue!20},
    process/.style={fill=green!20},
    output/.style={fill=red!20},
    arrow/.style={-Stealth, thick}
]

% Nodes
\node[input] (config) {Scene and Camera \\ Config (YAML)};
\node[input, below=of config] (pose) {Pose Space \\ (JSON) };
\node[process, right=of config] (rendering) {Abstract Rendering \\ (Gaussian Splat)};
\node[process, below=of rendering] (partition) {Partition Pose \\ Space };
\node[process, right=of rendering] (net) {CROWN \\ (GateNet)};
\node[output, right=of net] (results) {Verification Results \\ (Fig.~\ref{fig:verify_tracks})};

% Arrows
\draw[arrow] (config) -- (rendering) node[midway, above, draw=none] {camera params, \\ scene (Fig.~\ref{fig:scene}),\\ etc };
\draw[arrow] (pose) -- (partition) node[midway, above, draw=none] {Pose Space $P$ \\ (Fig.~\ref{fig:art-workflow} Left)};
\draw[arrow] (partition) -- (rendering) node[midway, right, draw=none] {Pose Cells $\{P_i\}$};
\draw[arrow] (rendering) -- (net) node[midway, above, draw=none] {Abstract Image $\{A_i\}$ \\ (Fig.~\ref{fig:art-workflow} Right)};
\draw[arrow] (net) -- (results) node[midway, above, draw=none] {Predicted Pose \\ $\{\hat{Y}_i\}$};
\draw[arrow] (partition) -| (results) node[pos=0.25, above, draw=none] {Pose Cells $\{P_i\}$};
% Labels

\node[above=0.5cm of config, draw=none] {User Inputs};
\node[above=0.5cm of results, draw=none] {Output};

\end{tikzpicture}%
}%
